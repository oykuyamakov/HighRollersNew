// using Animation;
// using Spine.Unity;
// using UnityCommon.Modules;
using UnityEngine;

namespace UI
{
	public class SpineAnimation : MonoBehaviour
	{
		// [SerializeField]
		// private AnimationReferenceAsset anim;
		//
		// private SkeletonGraphic skeletonAnimation;
		//
		//
		// private void Awake()
		// {
		// 	skeletonAnimation = GetComponent<SkeletonGraphic>();
		//
		// 	Conditional.WaitFrames(3).Do(() => { SetAnimation(anim, true, 1f); });
		// }
		//
		// //sets character animation
		// private void SetAnimation(AnimationReferenceAsset animation, bool loop, float timeScale)
		// {
		// 	skeletonAnimation.AnimationState.SetAnimation(0, animation, loop).TimeScale = timeScale;
		// }
	}
}
